# Infernux Engine - Python Dependencies

# Build dependencies
pybind11>=2.11.0
cmake>=3.25
build>=1.0.0
wheel>=0.41.0
setuptools>=61.0
nuitka[onefile]
auditwheel>=6.5; sys_platform == "linux"
patchelf>=0.17; sys_platform == "linux"

# Runtime dependencies
numpy>=1.21.0
numba>=0.61.0
watchdog>=1.0.0
Pillow>=10.4.0
imageio>=2.34.0
av>=12.0.0

# Launcher (editor project manager)
PySide6>=6.5.0

# Development / Testing
pytest>=7.0.0
-r external/plugins/infernux_mcp/package/requirements.txt
